{
  "name": "notnuzzel-scheduler",
  "version": "1.0.0",
  "scripts": {
    "dev": "dotenv ncc run worker.js",
    "build": "ncc build worker.js -o worker"
  },
  "dependencies": {
    "@notnuzzel/crawl": "^1.0.1",
    "@notnuzzel/prisma": "^1.0.5",
    "@prisma/client": "^2.22.1",
    "cron": "^1.8.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.28.5"
  }
}
